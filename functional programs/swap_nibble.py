def nibble_swap(number):
  """
  Description:
    This function simulates flipping a coin multiple times and returns the
    percentages of heads and tails.

  Parameters:
    coin_flip: The number of times the coin is flipped. Must be a positive integer.

  Returns:
    A tuple containing the percentages of tails and heads, respectively.
    If the input is invalid (less than 1), an error message is returned.
  """
  binary_number = convert_binary(number)

  return binary_number
def convert_binary(number):
  pass

def main():
  number = int(input("Enter a number"))
  print(nibble_swap(number))
if __name__=="__main__":
  main()